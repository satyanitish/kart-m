<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kartm">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KartM">
        <activity android:name=".ConfimationOrderActivity"></activity>
        <activity android:name=".RiceActivity" />
        <activity android:name=".BuyingProductActivity" />
        <activity android:name=".Wheat" />
        <activity android:name=".ListdataActivity" />
        <activity android:name=".AddinToCartActivity" />
        <activity android:name=".EditActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".SeedsLayout" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".RegisterActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
    </application>

</manifest>